import tkinter as tk

root = tk.Tk()
root.title("Tk App")

label = tk.Label(root, text="Welcome!")
label.pack(pady=20)

tk.Button(root, text="Click", command=lambda: label.config(text="Hello!")).pack(pady=10)

root.mainloop()
